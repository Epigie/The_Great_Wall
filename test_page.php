<?php session_start();
	  require_once('new-connection.php'); 
	
	#escape_this_string($_POST['post_message']);
	#$post_message = "INSERT INTO posts (post, created_at, updated_at, user_id) 
	#				 VALUES ( '$posts', NOW(), NOW(), {$_SESSION['user']['id']} )";
	#$get_messages = "SELECT * FROM users JOIN posts ON users.id = posts.user_id";
	// $get_posts = "SELECT * FROM posts";


	// echo "fetch all from posts:";
	// var_dump(fetch_all($get_posts));
	// echo "Echoing session: ";
	// var_dump($_SESSION);
	//   var_dump(fetch_record("select * from posts left join users on posts.user_id = users.id"));
	












 ?>
 <html>
 <head>
 	<title>test page</title>
 	<!-- <meta http-equiv="refresh" content="1"> -->
 </head>
 <body>
 
 </body>
 </html>